# assets

Put `Siddharth_Singh_CV.pdf` here to make the **View CV** button in the hero work.

The path is configured in [`../data/content.js`](../data/content.js) under
`profile.cv.url`. Setting `url` to an empty string hides the button entirely
instead of leaving a link that 404s.
